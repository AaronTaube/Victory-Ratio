Victory Ratio (working title) is a strategy game currently in development in python using the pygame framework.

Currently must be run from the console after downloading, but will later display in a web page.